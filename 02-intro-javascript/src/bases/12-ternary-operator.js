// Ternary Operator

const active = false;

// let message = '';
// if (active) {
//   message = 'Active';
// } else {
//   message = 'Inactive';
// }

const message = active ? 'Active' : 'Inactive';
console.log(message);

const result = active && 'Active';
console.log(result);